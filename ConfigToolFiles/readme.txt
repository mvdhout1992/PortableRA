See readme/build.txt for an explanation of what these files do. 

To install these files place them in the main Red Alert folder, note the following:

	- You need to rename ForceAMrules.ini and ForceCSrules.ini to rules.ini after placing it in the Red Alert folder.
	- the sc-*.mix files are coupled with acorresponding sc-*.pkt with the same name, both need to be installed in the Red Alert folder to work poperly.